# Backend API URL (Development)
# For local development, use localhost
VITE_API_URL=http://localhost:8000

# Production URL is configured in .env.production
# and used automatically by GitHub Actions during deployment
